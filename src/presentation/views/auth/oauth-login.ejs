<%# 
OAuth Enhanced Login Page - Sprint 04
Corporate OAuth-enabled login page with department selection and mobile optimization
Integrates with landing page system and supports corporate branding
%>

<%- include('../templates/auth-layout', {
    title: (typeof corporateConfig !== 'undefined' && corporateConfig && corporateConfig.name)
           ? 'Sign in to ' + corporateConfig.name + ' - Amexing'
           : 'Sign In - Amexing',
    page: 'oauth-login',
    showAuthHeader: true,
    headerConfig: {
        title: (typeof corporateConfig !== 'undefined' && corporateConfig && corporateConfig.name)
               ? 'Welcome to ' + corporateConfig.name
               : 'Choose Your Sign In Method',
        subtitle: 'Select your preferred authentication method to continue',
        corporate: typeof corporateConfig !== 'undefined' ? corporateConfig : null
    },
    content: include('../organisms/auth/corporate-oauth-section', {
        corporate: typeof corporateConfig !== 'undefined' ? corporateConfig : null,
        departments: typeof departments !== 'undefined' ? departments : [],
        requireDepartment: typeof departmentRequired !== 'undefined' ? departmentRequired : false,
        showPersonalOption: typeof allowPersonalAuth !== 'undefined' ? allowPersonalAuth : true,
        action: 'login'
    }),
    error: typeof error !== 'undefined' ? error : null,
    success: typeof success !== 'undefined' ? success : null,
    csrfToken: typeof csrfToken !== 'undefined' ? csrfToken : '',
    user: typeof user !== 'undefined' ? user : null
}) %>
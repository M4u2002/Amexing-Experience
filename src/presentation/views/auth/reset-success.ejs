<%- include('../layout', {
    body: '<div style="text-align: center; padding: 3rem 0;">' +
          '<div style="font-size: 4rem; color: #3c3; margin-bottom: 1rem;">✓</div>' +
          '<h2 style="color: #333; margin-bottom: 1rem;">' + (typeof title !== "undefined" ? title : "Password Reset Successful") + '</h2>' +
          '<p style="color: #666; margin-bottom: 2rem;">' + (typeof message !== "undefined" ? message : "Your password has been successfully reset.") + '</p>' +
          '<a href="/login" class="btn">Login with New Password</a>' +
          '</div>'
}) %>
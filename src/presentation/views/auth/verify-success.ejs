<%- include('../layout', {
    body: '<div style="text-align: center; padding: 3rem 0;">' +
          '<div style="font-size: 4rem; color: #3c3; margin-bottom: 1rem;">✓</div>' +
          '<h2 style="color: #333; margin-bottom: 1rem;">' + (typeof title !== "undefined" ? title : "Email Verified Successfully") + '</h2>' +
          '<p style="color: #666; margin-bottom: 2rem;">' + (typeof message !== "undefined" ? message : "Your email has been successfully verified.") + '</p>' +
          '<a href="/login" class="btn">Go to Login</a>' +
          '</div>'
}) %>
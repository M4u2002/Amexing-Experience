<% var resetPasswordContent = include('../partials/reset-password', { error: error, token: token }); %>
<%- include('../layout', {
    title: 'Reset Password',
    body: resetPasswordContent,
    error: error
}) %>
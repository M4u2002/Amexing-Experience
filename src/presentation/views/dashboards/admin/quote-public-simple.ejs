<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="robots" content="noindex, nofollow">
    <title><%= pageTitle || 'Cotización' %></title>

    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico">

    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f5f5f5;
            padding: 2rem 0;
        }

        .public-quote-container {
            max-width: 1200px;
            margin: 0 auto;
            background: #fff;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            border-radius: 8px;
            padding: 2rem;
        }

        .summary-banner {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 2rem;
            border-bottom: 3px solid #0d6efd;
            margin-bottom: 2rem;
        }

        .summary-banner-logo img {
            max-width: 250px;
            height: auto;
        }

        .summary-banner-address {
            text-align: right;
            color: #495057;
            line-height: 1.6;
        }

        .summary-banner-address div {
            margin-bottom: 0.25rem;
        }

        .public-header {
            text-align: center;
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid #0d6efd;
        }

        .public-header h1 {
            color: #0d6efd;
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .public-header p {
            color: #6c757d;
            font-size: 1rem;
            margin: 0;
        }

        .info-section {
            margin-bottom: 2rem;
            padding: 1.5rem;
            background: #f8f9fa;
            border-radius: 0.5rem;
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1rem;
        }

        .info-item {
            display: flex;
            flex-direction: column;
        }

        .info-label {
            font-weight: 600;
            color: #6c757d;
            font-size: 0.875rem;
            margin-bottom: 0.25rem;
        }

        .info-value {
            color: #212529;
            font-size: 1rem;
        }

        table {
            width: 100%;
            margin-bottom: 1rem;
        }

        th {
            background: #0d6efd;
            color: #fff;
            font-weight: 600;
            padding: 0.75rem;
            text-align: left;
        }

        td {
            padding: 0.75rem;
            border-bottom: 1px solid #dee2e6;
        }

        .text-end {
            text-align: right;
        }

        .totals-row {
            font-weight: 600;
        }

        .public-footer {
            text-align: center;
            margin-top: 3rem;
            padding-top: 2rem;
            border-top: 1px solid #dee2e6;
            color: #6c757d;
            font-size: 0.875rem;
        }

        .public-footer a {
            color: #0d6efd;
            text-decoration: none;
        }

        .public-footer a:hover {
            text-decoration: underline;
        }

        /* Print styles */
        @media print {
            body {
                background: #fff;
                padding: 0;
            }

            .public-quote-container {
                box-shadow: none;
                border-radius: 0;
                padding: 0;
            }

            .public-header {
                display: none;
            }

            .public-footer {
                display: none;
            }

            .summary-banner {
                page-break-after: avoid;
            }
        }

        /* Responsive styles */
        @media (max-width: 768px) {
            .summary-banner {
                flex-direction: column;
                text-align: center;
            }

            .summary-banner-address {
                text-align: center;
                margin-top: 1rem;
            }

            .summary-banner-logo img {
                max-width: 200px;
            }
        }
    </style>
</head>
<body>
    <div class="public-quote-container">
        <!-- Banner with Logo and Address -->
        <div class="summary-banner">
            <div class="summary-banner-logo">
                <img src="/img/amexing_logo_horizontal.avif" alt="Amexing Experience">
            </div>
            <div class="summary-banner-address">
                <div>Vicente Suarez 5, Colonia Independencia</div>
                <div>San Miguel de Allende, Guanajuato 37732</div>
                <div>+52 (415) 167 39 90</div>
                <div>contact@amexingexperience.com</div>
            </div>
        </div>

        <!-- Public Header -->
        <div class="public-header">
            <h1><%= quote.folio %></h1>
            <p>Cotización de Servicios</p>
        </div>

        <!-- Quote Information -->
        <div class="info-section">
            <h3 class="mb-3">Información General</h3>
            <div class="info-grid">
                <% if (quote.client) { %>
                <div class="info-item">
                    <span class="info-label">Cliente</span>
                    <span class="info-value"><%= quote.client.firstName %> <%= quote.client.lastName %></span>
                </div>
                <% } %>

                <% if (quote.numberOfPeople) { %>
                <div class="info-item">
                    <span class="info-label">Número de Personas</span>
                    <span class="info-value"><%= quote.numberOfPeople %></span>
                </div>
                <% } %>

                <% if (quote.eventType) { %>
                <div class="info-item">
                    <span class="info-label">Tipo de Evento</span>
                    <span class="info-value"><%= quote.eventType %></span>
                </div>
                <% } %>

                <%# Tarifa ocultada - no debe exponerse en vista pública - Eliminado completamente por seguridad %>
            </div>
        </div>

        <!-- Services Table -->
        <% if (quote.serviceItems && quote.serviceItems.days && quote.serviceItems.days.length > 0) { %>
        <div class="info-section">
            <h3 class="mb-3">Itinerario de Servicios</h3>

            <% quote.serviceItems.days.forEach(function(day) { %>
                <h5 class="mt-3 mb-2">Día <%= day.dayNumber %> <% if (day.city) { %> - <%= day.city %><% } %></h5>

                <% if (day.subconcepts && day.subconcepts.length > 0) { %>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Concepto</th>
                            <th>Tipo de Servicio</th>
                            <th>Tipo de Vehículo</th>
                            <th class="text-end">Horas</th>
                            <th class="text-end">Precio Unitario</th>
                            <th class="text-end">Total</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% day.subconcepts.forEach(function(sub) { %>
                        <tr>
                            <td><%= sub.concept || '-' %></td>
                            <td><%= sub.serviceType || '-' %></td>
                            <td>
                                <% if (sub.vehicleMultiplier && sub.vehicleMultiplier > 1) { %>
                                    <span class="badge bg-info"><%= sub.vehicleMultiplier %>x</span>
                                <% } %>
                                <%= sub.vehicleType || '-' %>
                            </td>
                            <td class="text-end"><%= sub.hours || 0 %></td>
                            <td class="text-end">$<%= (sub.unitPrice || 0).toLocaleString('es-MX', { minimumFractionDigits: 2 }) %></td>
                            <td class="text-end">$<%= (sub.total || 0).toLocaleString('es-MX', { minimumFractionDigits: 2 }) %></td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
                <% } %>
            <% }); %>

            <!-- Totals -->
            <table class="table">
                <tbody>
                    <tr>
                        <td colspan="5" class="text-end">Subtotal:</td>
                        <td class="text-end">$<%= (quote.serviceItems.subtotal || 0).toLocaleString('es-MX', { minimumFractionDigits: 2 }) %></td>
                    </tr>
                    <tr>
                        <td colspan="5" class="text-end">IVA (16%):</td>
                        <td class="text-end">$<%= (quote.serviceItems.iva || 0).toLocaleString('es-MX', { minimumFractionDigits: 2 }) %></td>
                    </tr>
                    <tr class="totals-row">
                        <td colspan="5" class="text-end">Total:</td>
                        <td class="text-end">$<%= (quote.serviceItems.total || 0).toLocaleString('es-MX', { minimumFractionDigits: 2 }) %></td>
                    </tr>
                </tbody>
            </table>
        </div>
        <% } else { %>
        <div class="info-section">
            <p class="text-muted">No hay servicios agregados a esta cotización.</p>
        </div>
        <% } %>

        <!-- Public Footer -->
        <div class="public-footer">
            <p>
                Esta cotización fue generada por <strong>Amexing</strong><br>
                Para más información, visite
                <a href="https://amexing.com" target="_blank" rel="noopener">www.amexing.com</a>
            </p>
        </div>
    </div>

    <!-- Bootstrap 5 JS (for responsive features) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

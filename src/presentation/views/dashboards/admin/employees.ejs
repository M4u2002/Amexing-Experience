<%
/**
 * Admin Employees Management Page
 * Manages Amexing internal employees (employee_amexing role)
 *
 * Layout Structure:
 * - Direct row/column structure
 * - Container-fluid from Flexy Bootstrap
 * - DataTables for employee listing
 *
 * Note: DataTables CSS/JS libraries are loaded via controller
 */

// Page configuration
const currentUserRole = typeof userRole !== 'undefined' ? userRole : 'admin';
const currentPath = '/dashboard/admin/employees';
%>

<!-- Page Header with Breadcrumb -->
<div class="row">
    <div class="col-12">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h2 class="mb-1 fw-bold">Gestión de Empleados</h2>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb mb-0">
                        <li class="breadcrumb-item">
                            <a href="/dashboard/admin" class="text-decoration-none">
                                <i class="ti ti-home me-1"></i>Dashboard
                            </a>
                        </li>
                        <li class="breadcrumb-item active" aria-current="page">
                            <i class="ti ti-users me-1"></i>Empleados
                        </li>
                    </ol>
                </nav>
            </div>
            <div class="text-muted">
                <small class="d-block">Total empleados registrados</small>
                <strong id="total-employees-count">-</strong>
            </div>
        </div>
    </div>
</div>

<!-- Employees DataTable -->
<div class="row">
    <div class="col-12">
        <%- include('../../organisms/datatable/amexing-employees-table', {
            tableId: 'employees-table',
            apiEndpoint: '/api/employees',
            userRole: currentUserRole,
            showActions: true
        }) %>
    </div>
</div>

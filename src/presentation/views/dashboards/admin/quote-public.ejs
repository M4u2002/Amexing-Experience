<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="robots" content="noindex, nofollow">
    <title><%= pageTitle || 'Cotización' %></title>

    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico">

    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f5f5f5;
            padding: 2rem 0;
        }

        .public-quote-container {
            max-width: 1200px;
            margin: 0 auto;
            background: #fff;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            border-radius: 8px;
            padding: 2rem;
        }

        .public-header {
            text-align: center;
            margin-bottom: 2rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid #0d6efd;
        }

        .public-header h1 {
            color: #0d6efd;
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .public-header p {
            color: #6c757d;
            font-size: 1rem;
            margin: 0;
        }

        .public-footer {
            text-align: center;
            margin-top: 3rem;
            padding-top: 2rem;
            border-top: 1px solid #dee2e6;
            color: #6c757d;
            font-size: 0.875rem;
        }

        .public-footer a {
            color: #0d6efd;
            text-decoration: none;
        }

        .public-footer a:hover {
            text-decoration: underline;
        }

        /* Print styles */
        @media print {
            body {
                background: #fff;
                padding: 0;
            }

            .public-quote-container {
                box-shadow: none;
                border-radius: 0;
                padding: 0;
            }

            .public-header,
            .public-footer {
                display: none;
            }
        }
    </style>
</head>
<body>
    <div class="public-quote-container">
        <!-- Public Header -->
        <div class="public-header">
            <h1><%= quote.folio %></h1>
            <p>Cotización de Servicios</p>
        </div>

        <!-- Include quote-summary section (reuse existing template) -->
        <%- include('./sections/quote-summary', {
            quote: quote,
            isPublicView: true
        }) %>

        <!-- Public Footer -->
        <div class="public-footer">
            <p>
                Esta cotización fue generada por <strong>Amexing</strong><br>
                Para más información, visite
                <a href="https://amexing.com" target="_blank" rel="noopener">www.amexing.com</a>
            </p>
        </div>
    </div>

    <!-- Bootstrap 5 JS (for responsive features) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
